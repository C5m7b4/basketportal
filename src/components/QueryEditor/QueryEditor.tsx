import React, { useState, useEffect } from 'react';

import { findColor, htmlEncode, copyTextToClipboard } from './utils';
import {
  reservedWords,
  appWords,
  defaultWords,
  alternateWords,
  operators,
} from './reserved';
const QueryEditor = () => {};

export default QueryEditor;
